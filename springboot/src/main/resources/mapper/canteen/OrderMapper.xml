<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fast.canteen.mapper.OrderMapper">

    <resultMap type="Order" id="OrderResult">
        <result property="orderId"    column="order_id"    />
        <result property="totalPrice"    column="total_price"    />
        <result property="status"    column="status"    />
        <result property="userId"    column="user_id"    />
        <result property="createTime"    column="create_time"    />
        <result property="userName"    column="user_name"    />
    </resultMap>

    <resultMap id="OrderOrderOdResult" type="Order" extends="OrderResult">
        <collection property="orderOdList" ofType="OrderOd" column="order_id" select="selectOrderOdList" />
    </resultMap>

    <resultMap type="OrderOd" id="OrderOdResult">
        <result property="odId"    column="od_id"    />
        <result property="orderId"    column="order_id"    />
        <result property="name"    column="name"    />
        <result property="image"    column="image"    />
        <result property="quantity"    column="quantity"    />
        <result property="price"    column="price"    />
    </resultMap>

    <select id="selectOrderList" parameterType="Order" resultMap="OrderOrderOdResult">
        select o.order_id, o.total_price, o.status, o.user_id, o.create_time,
               u.user_name
        from `order` o
        left join sys_user u on o.user_id = u.user_id
        <where>
            <if test="orderId != null  and orderId != ''"> and o.order_id = #{orderId}</if>
            <if test="status != null  and status != ''"> and o.status = #{status}</if>
            <if test="userId != null "> and o.user_id like concat('%', #{userId}, '%')</if>
            <if test="userName != null "> and u.user_name like concat('%', #{userName}, '%')</if>
        </where>
        order by o.create_time desc
    </select>

    <select id="selectOrderByOrderId" parameterType="String" resultMap="OrderOrderOdResult">
        select order_id, total_price, status, user_id, create_time
        from `order`
        where order_id = #{orderId}
    </select>

    <select id="selectOrderOdList" resultMap="OrderOdResult">
        select od_id, order_id, name, image, quantity, price
        from order_od
        where order_id = #{order_id}
    </select>

    <!--查询订单总金额-->
    <select id="selectOrderTotalAmount" resultType="java.math.BigDecimal">
        select coalesce(sum(total_price), 0)
        from `order`
        where status = '已完成'
    </select>

    <!--查询指定日期的销售金额-->
    <select id="selectSalesAmountByDate" resultType="java.math.BigDecimal">
        select coalesce(sum(total_price), 0)
        from `order`
        where date(create_time) = #{date} and status = '已完成'
    </select>

    <!--查询订单状态分布数据-->
    <select id="selectOrderStatusChart" resultType="com.fast.canteen.domain.vo.PieVO">
        select status as name, count(order_id) as value
        from `order`
        group by status
    </select>

    <insert id="insertOrder" parameterType="Order">
        insert into `order`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="orderId != null">order_id,</if>
            <if test="totalPrice != null">total_price,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="userId != null">user_id,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="orderId != null">#{orderId},</if>
            <if test="totalPrice != null">#{totalPrice},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="userId != null">#{userId},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateOrder" parameterType="Order">
        update `order`
        <trim prefix="SET" suffixOverrides=",">
            <if test="totalPrice != null">total_price = #{totalPrice},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where order_id = #{orderId}
    </update>

    <delete id="deleteOrderByOrderId" parameterType="String">
        delete from `order` where order_id = #{orderId}
    </delete>

    <delete id="deleteOrderByOrderIds" parameterType="String">
        delete from `order` where order_id in
        <foreach item="orderId" collection="array" open="(" separator="," close=")">
            #{orderId}
        </foreach>
    </delete>

    <delete id="deleteOrderOdByOrderIds" parameterType="String">
        delete from order_od where order_id in
        <foreach item="orderId" collection="array" open="(" separator="," close=")">
            #{orderId}
        </foreach>
    </delete>

    <delete id="deleteOrderOdByOrderId" parameterType="String">
        delete from order_od where order_id = #{orderId}
    </delete>

    <insert id="batchOrderOd">
        insert into order_od( od_id, order_id, name, image, quantity, price) values
        <foreach item="item" index="index" collection="list" separator=",">
            ( #{item.odId}, #{item.orderId}, #{item.name}, #{item.image}, #{item.quantity}, #{item.price})
        </foreach>
    </insert>
</mapper>
